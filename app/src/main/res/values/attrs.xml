<?xml version="1.0" encoding="utf-8"?>
<resources>
    <attr name="drawCap" format="boolean" />
    <attr name="progress" format="float" />

    <declare-styleable name="JuicyFillingRingView">
        <attr name="ringFillColor" format="color" />
        <attr name="progress" />
        <attr name="diameterFraction" format="float" />
        <attr name="drawCap" />
        <attr name="capWidth" format="integer" />
        <attr name="capLength" format="integer" />
    </declare-styleable>

    <declare-styleable name="JuicySkillNodeView">
        <attr name="progress" />
    </declare-styleable>

    <!-- AppCompatImageView  todo What does this do?-->
    <attr name="aspectRatio" format="float" />

</resources>